{
  "private": true,
  "productName": "IRCCloud",
  "version": "0.7.0",
  "author": {
    "name": "LYK",
    "email": "dalinaum@gmail.com"
  },
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "rm -rf dist; mkdir dist; ./generate_icon; ./build-mac $npm_package_productName $npm_package_version",
    "test": "rm -rf dist; mkdir dist; ./generate_icon; ./build-mac $npm_package_productName $npm_package_version"
  },
  "devDependencies": {
    "electron-packager": "^4.1.3",
    "electron-prebuilt": "^0.28.3"
  }
}
